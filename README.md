# FailMock public documentation

FailMock helps developers test how your app behaves when an external API fails.


The basic flow is simple:

```text
Install FailMock -> create failmock.yaml -> run the server -> call the fake provider -> see the failure -> fix your app behavior
```

## Start here

1. [Start here](docs/start-here.md)